#pragma once

const unsigned int WINDOW_WIDTH = 800;
const unsigned int WINDOW_HEIGHT = 600;
const char* WINDOW_TITLE = "Mithras Engine";

const int FPS = 60; // 60 frames per second
const float TIME_PER_FRAME = 1000.0 / FPS;

